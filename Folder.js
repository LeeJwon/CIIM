import React, { Component } from 'react';
import { StyleSheet, Text, View, Button, Image } from 'react-native';
import { ScreenContainer } from 'react-native-screens';

export const Folder = ({ navigation }) => {
  return (
    <ScreenContainer>
    </ScreenContainer>
  );
};

